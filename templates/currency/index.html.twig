{% extends 'base.html.twig' %}

{% block title %}Hello CurrencyController!{% endblock %}

{% block body %}
<h1>Currencies</h1>
{% if currencies %}
    <table id="tbl_currencies" class="table table-striped">
        {% for c in currencies %}
        <tr>
            <td>{{ c.name }}</td>
            <td><a href="#" class="btn btn-danger delete-item" data-id="{{ c.id }}" data-name="{{ c.name }}">Remove</a></td>
        </tr>
        {% endfor %}
    </table>
{% endif %}
{% endblock %}

{% block bodyscripts %}
<script>
const table = document.getElementById('tbl_currencies');

if (table) {
  table.addEventListener('click', e => {
    if (e.target.className === 'btn btn-danger delete-item') {
      const name = e.target.getAttribute('data-name');
      if (confirm(`Do you really want to delete ${name}?`)) {
        const id = e.target.getAttribute('data-id');

        fetch(`{{ app.request.getBaseURL() }}/currency/delete/${id}`, {
          method: 'DELETE'
        }).then(res => window.location.reload());
      }
    }
  });
}
</script>
{% endblock %}
